# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
env/
venv/
.venv/
ENV/
develop-eggs/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Environment — contains real API keys, never commit
.env
.env.local
.env.*.local
docker-compose.dev.yml

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Data
/data/
*.db
*.sqlite

# Logs
*.log

# Node
node_modules/
npm-debug.log*

# Build artifacts (distributed via GitHub Releases)
dist/

# Bundled runtime — downloaded/assembled by prepare-build.sh, never committed
bundled-python/
bundled-packages/
bundled-tools/

# Docker override
docker-compose.override.yml

# Local-only dev helpers & scratch notes (contain machine-specific paths)
reinstall.sh
REINSTALL-*.md
unistal.md
app-improvement-review.md
build-and-push.md

# Planning docs / roadmaps / scan notes are scaffolding, not product — they
# never belong in the repo (Features.md shipped by mistake in v1.12.10).
Features.md
SCAN_NOTES.md

# tools/ is dev-only; allow only what is explicitly whitelisted below
tools/*
!tools/ui-geometry-check.js

# Editor/agent local state — never product
.claude/
